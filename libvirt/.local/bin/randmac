#!/bin/sh

# abstract: generates a random mac address

if [ $# -eq 0 ]; then
    # fully random
    openssl rand -hex 6 | sed 's/\(..\)/\1:/g; s/:$//'
else
    # reproducible
    echo $1 | md5sum | sed 's/^\(..\)\(..\)\(..\)\(..\)\(..\).*$/02:\1:\2:\3:\4:\5/'
fi
