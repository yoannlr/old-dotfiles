#!/bin/sh

tab=$(cat "$1")

while [ 1 ]
do
	echo "Cron loop"
	echo ${tab} | grep -e "$(date '+\*|%M%t\*|%H%t\*|%d%t\*|%m%t\*|%w%t.*')" | while read line
	do
		cmd="$(echo ${line} | cut -d$'\t' -f6)"
		$cmd &
	done
	sleep 60
done
