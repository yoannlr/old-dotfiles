#!/bin/sh

# abstract: adds new songs to the current mpd playlist

temp="/tmp/mpctmp.${USER}"

mpc rescan
sleep 2
mpc listall | sort > "$temp"
sleep 2
mpc -f '%file%' playlist | sort | comm -13 - "$temp" | xargs -I path mpc --verbose add 'path'

rm "$temp"
